#include <stdio.h>

int max(int a, int b);
int min(int a, int b);

int main(void)
{
	printf("Task1_Execute!!!\n");

	printf("max(5, 2) : %d\n", max(5, 2));
	printf("min(5, 2) : %d\n", min(5, 2));

	return 0;
}
